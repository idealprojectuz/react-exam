import { createTheme } from "@mui/material";
const theme = createTheme({
    palette: {
        primary: {
            main: '#5A5AC9', // Change this to customize the primary color
        },
        outlined: {
            main: '#5A5AC9',
        },
    },
});

export default theme;